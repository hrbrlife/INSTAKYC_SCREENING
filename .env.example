# Example environment configuration for the InstaKYC Screening MVP stack.
# Copy this file to .env and replace the placeholder value with your API key.

# Single API key shared across all requests and the container runtime.
SCREENING_API_KEY=replace-with-your-api-key
# The FastAPI service also reads API_KEY for backwards compatibility.
API_KEY=replace-with-your-api-key
